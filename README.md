# User Registration (Pydantic v2)

Система регистрации пользователя с валидацией вложенных структур (Address) и сериализацией JSON.

## Требования
- Python 3.10+
- Pydantic v2 (`pip install "pydantic[email]"`)

## Запуск
```bash
python main.py